using Godot;
using System;

public class Main : Node {
    public override void _Ready() {
        // if (OS.HasTouchscreenUiHint()) {
        //     GetNode<Sprite>("test").Visible = true;
        // }
    }
}
